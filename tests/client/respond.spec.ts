describe('CTProtoClient', () => {
  describe('Responds message id', () => {
    it('should be respond message id should be response message id', () => {
      /**
       * Can't implement this test yet:
       * we didn't implement this method
       */
      expect(true).toBe(true);
    });
  });
});
