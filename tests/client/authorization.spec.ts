describe('CTProtoClient', () => {
  describe('SendAuthorizationMessage', () => {
    it('should be send authorization message in 3 sec after connection', () => {
      /**
       * Can't implement this test yet:
       * we didn't implement this method
       */
      expect(true).toBe(true);
    });
  });
});
